spring:
  application:
    name: ms-funcionario
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: false
  datasource:
    url: jdbc:h2:mem:dbfuncionariotest
    driver-class-name: org.h2.Driver
    username: admin
    password: admin
  h2:
    console:
      enabled: true
      path: /h2-console